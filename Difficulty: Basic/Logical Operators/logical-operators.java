class Solution {
    public String booleanOperations(boolean a, boolean b) {
        // Code here
        //not condition no loop
        //bs ek print statement me write this
        return (a&&b)+" "+(a||b)+" "+(!a);
    }
}